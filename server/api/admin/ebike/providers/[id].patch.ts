import { getDb, now } from '../../../../db/index'
export default defineEventHandler(async event=>{const s=await requireUserSession(event);if(s.user.role!=='admin')throw createError({statusCode:403});const id=Number(getRouterParam(event,'id'));const b=await readBody<Record<string,any>>(event);const fields=['name','contact_name','contact_email','whatsapp','notes','active','rental_enabled','rental_daily_cost','rental_daily_margin','rental_exceptions','pickup_location_id','return_location_id'];const sets:string[]=[];const vals:any[]=[];for(const f of fields)if(b[f]!==undefined){sets.push(`${f}=?`);vals.push(f==='rental_exceptions'?JSON.stringify(b[f]):b[f])}sets.push('updated_at=?');vals.push(now(),id);getDb().prepare(`UPDATE partners SET ${sets.join(',')} WHERE id=? AND category='ebike'`).run(...vals);return{success:true}})
